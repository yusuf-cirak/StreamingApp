import { TextHashColorDirective } from './text-hash-color.directive';

describe('TextHashColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHashColorDirective();
    expect(directive).toBeTruthy();
  });
});
